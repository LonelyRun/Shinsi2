excluded: # paths to ignore during linting. Takes precedence over `included`.
  - Carthage
  - Pods
disabled_rules:
  - trailing_whitespace
  - type_name
  - force_cast
  - force_try
  - file_length
  - variable_name
  - function_body_length
identifier_name:
  min_length: 1
line_length: 300
